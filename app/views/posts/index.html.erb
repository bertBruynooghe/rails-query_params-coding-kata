<p id="notice"><%= notice %></p>

Query:
<%= @posts.to_sql %>
<br>

Params:
<%= params %>

<br>

<%= link_to('title_starting_with_an_a', params.merge(title_starting_with: 'a')) %>
<br>
<%= link_to('toggle title sort',params.merge(reorder: {title: next_sort_order_for(:title)})) %>

<form>
    <%= label_tag(:title_starting_with, 'title starting with') %>
    <%= text_field_tag(:title_starting_with, params[:title_starting_with]) %>
</form>

<form>
    <%= label_tag(:page, 'page') %>
    <%= text_field_tag(:page, params[:page]) %>
</form>

<%= will_paginate @posts %>
<%= link_to 'New Post', new_post_path %>
